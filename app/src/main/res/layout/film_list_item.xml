<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/film_item_icon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:contentDescription="@string/image_of_the_film_content_description"
        android:padding="4dp"
        android:scaleType="fitCenter" />

    <TextView
        android:id="@+id/film_item_title"
        style="@style/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/film_item_icon"
        android:padding="4dp" />

    <TextView
        android:id="@+id/film_item_info"
        style="@style/info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/film_item_title"
        android:layout_alignParentBottom="true"
        android:padding="4dp" />

</RelativeLayout>